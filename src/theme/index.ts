export { colors, radius } from './colors';
export { fonts, type, tracking } from './typography';

/** Padding horizontal del cuerpo de todas las pantallas (`.body` en el mockup) */
export const SCREEN_PADDING = 21;
